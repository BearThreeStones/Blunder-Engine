# Resources

Raw, uncompiled source files used as importer input (models, textures, audio, fonts).

Runtime loading uses virtual paths with the `resources/` prefix, for example
`resources/Models/textured_cube/textured_cube.gltf`.

Art source files (`.psd`, etc.) may live under `Source/` and are not loaded at runtime.
