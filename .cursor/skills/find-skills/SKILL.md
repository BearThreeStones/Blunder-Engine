---
name: find-skills
description: Helps users discover and install agent skills when they ask questions like "how do I do X", "find a skill for X", "is there a skill that can...", or express interest in extending capabilities. This skill should be used when the user is looking for functionality that might exist as an installable skill.
---

This skill is the git submodule file `.cursor/vendor/vercel-labs-skills/skills/find-skills/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/find-skills/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
