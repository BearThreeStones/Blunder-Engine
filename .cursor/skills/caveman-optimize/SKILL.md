---
name: caveman-optimize
description: >
  Turn a Caveman optimization observation into an operator-chosen candidate
  with a paired baseline evaluation. Use when asked to inspect or evaluate a
  Caveman optimization report. Needs explicit approval.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-optimize/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-optimize/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
