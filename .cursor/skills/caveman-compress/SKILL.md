---
name: caveman-compress
description: >
  Compress a memory file such as CLAUDE.md or a todo list into caveman format
  to save input tokens, keeping a readable backup. Trigger: /caveman-compress.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-compress/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-compress/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
