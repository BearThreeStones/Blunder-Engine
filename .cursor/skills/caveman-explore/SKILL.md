---
name: caveman-explore
description: Read-only repository explorer for cold-start orientation, broad cross-file localization, or when a direct search failed. Skip it when the exact file or symbol is already named. Returns path:line citations only; its reads stay out of main context.
tools: Read, Glob, Grep
model: haiku
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-explore/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-explore/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
