---
name: safe-refactor
description: Restructure code while preserving behavior. Use for extraction, consolidation, ownership moves, or cleanup where verification must bracket structural edits.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/safe-refactor/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/safe-refactor/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
