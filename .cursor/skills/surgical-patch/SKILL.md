---
name: surgical-patch
description: Fix bugs and small behavior changes at the narrowest responsible layer. Use when regression proof, preserved surrounding behavior, and task-relevant tests matter.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/surgical-patch/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/surgical-patch/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
