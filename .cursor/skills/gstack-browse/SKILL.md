---
name: browse
preamble-tier: 1
version: 2.0.0
description: "Drive a real browser through Aside: open a page, read it, click through a flow, take screenshots, check console errors. (gstack)"
triggers:
  - browse a page
  - open this url
  - take page screenshot
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
---

This skill is the git submodule file `.cursor/vendor/gstack/browse/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/gstack-browse/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
