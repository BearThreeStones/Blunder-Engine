---
name: caveman-discover
description: >
  Find and label every LLM workflow in the repository so Caveman Cloud groups
  spend by workflow instead of one bucket. Use for "discover workflows" or
  breaking LLM spend down by workflow.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-discover/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-discover/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
