---
name: caveman-commit
description: >
  Write a Conventional Commits message compressed to intent only. Use for
  "write a commit", "commit message", /commit or /caveman-commit.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-commit/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-commit/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
