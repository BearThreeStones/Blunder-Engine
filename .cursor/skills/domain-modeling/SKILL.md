---
name: domain-modeling
description: Build and sharpen a project's domain model. Use when discussing codebase terminology, writing or editing a CONTEXT.md, or recording or editing an ADR.
---

This skill is the git submodule file `.cursor/vendor/mattpocock-skills/skills/engineering/domain-modeling/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/domain-modeling/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
