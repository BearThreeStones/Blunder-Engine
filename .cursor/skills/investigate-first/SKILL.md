---
name: investigate-first
description: Diagnose ambiguous failures before editing. Use for unknown causes, intermittent behavior, performance regressions, or investigations needing evidence-ranked hypotheses.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/investigate-first/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/investigate-first/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
