---
name: caveman-setup
description: >
  Wire a repository through the Caveman Cloud gateway so every LLM request is
  measured, with no behavior change. Use for "set up caveman" or adding LLM
  spend observability.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-setup/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-setup/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
