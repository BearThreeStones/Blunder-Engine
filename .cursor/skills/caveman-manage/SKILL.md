---
name: caveman-manage
description: >
  Inspect Caveman Cloud's experiment lifecycle and block unsafe execution. Use
  when asked to start, approve, cancel, promote or roll back a Caveman
  experiment.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-manage/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-manage/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
