---
name: gstack-upgrade
version: 1.1.0
description: Upgrade gstack to the latest version.
triggers:
  - upgrade gstack
  - update gstack version
  - get latest gstack
allowed-tools:
  - Bash
  - Read
  - Write
  - AskUserQuestion
---

This skill is the git submodule file `.cursor/vendor/gstack/gstack-upgrade/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/gstack-upgrade/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
