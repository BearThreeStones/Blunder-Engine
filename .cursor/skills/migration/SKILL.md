---
name: migration
description: Implement reversible compatibility-safe transitions. Use for schema, data, API, protocol, configuration, or dependency migrations requiring rollback and preservation proof.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/migration/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/migration/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
