---
name: scrape
preamble-tier: 1
version: 2.0.0
description: Pull data from a web page through the Aside browser — your real, already signed-in sessions. (gstack)
allowed-tools:
  - Bash
  - Read
  - AskUserQuestion
triggers:
  - scrape this page
  - get data from
  - pull from
  - extract from
  - what is on
---

This skill is the git submodule file `.cursor/vendor/gstack/scrape/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/gstack-scrape/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
