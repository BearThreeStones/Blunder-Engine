---
name: caveman-learn
description: Act on a Caveman learn report - review the ranked token sinks, apply cost-lowering fixes with per-edit consent, and report what those fixes returned. Use when asked to lower an agent's token cost, what caveman has saved, to trim a heavy CLAUDE.md, or to offload re-pasted context into cavemem.
---

This skill is the git submodule file `.cursor/vendor/caveman/skills/caveman-learn/SKILL.md`. Read that file now and follow it in full. This stub exists so Cursor discovers the skill under `.cursor/skills/caveman-learn/`. Update: `git submodule update --remote` on the vendor checkout, then commit the new pin.
