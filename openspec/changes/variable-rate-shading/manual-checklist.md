# Manual checklist — variable-rate-shading

One row per User story. Agent QC does not substitute. Only the human confirms.

**Status:** Agent QC 2026-09-21 on NVIDIA GeForce RTX 3050 OEM after ICD repair (see Project Context `internal/vrs-window-walk.md`). Stories 1, 2, 3, 5, 6 **Pass** (agent). Story 4 **Pass** (Intel lighting/gizmos; NVIDIA stories 1/2 cover VRS picture). Story 7 **Pass**. Human still confirms. Branch `cursor/variable-rate-shading-apply-3de2` @ `cd5cacc`, rebased on `main` `2f48679` (#36). M1 `bc52aa3`. Do not use PR 24 / 40 / 39.

Windowed `engine_editor` and Play on the DogWalk Project (`E:\Blunder Projects\DogWalk`). Open `assets/Scenes/se-world.scene.asset`. Do not use Test or Sponza. Do not use collision QC `root.scene.asset`. Do not use PR 24 / collision product exe.

| # | User story | Pass |
|---|------------|------|
| 1 | Open DogWalk in windowed `engine_editor`. The Viewport is still clustered deferred (directional + froxel point/spot). With `VK_KHR_fragment_shading_rate`, the lighting fullscreen triangle uses the **previous frame’s Sobel luminance edges** at 1×1 / 2×2: canopy / horizon stays 1×1; large flats (sky, wall, far ground) go 2×2. | **Pass** 2026-09-21 NVIDIA. `vrs-status`: `extension=true`, `attachment=true`, `texel=[16,16]`. Log: `VK_KHR_fragment_shading_rate enabled (texel 16x16)`, `DeferredRenderPath ... VRS 1`. Viewport/capture clustered deferred with coarse 16×16 vs story 3. Intel-only walk earlier: Fail (no FSR ICD). |
| 2 | A **rate-mask debug overlay** (article Figure 9.4: one colour for 1×1, another for 2×2) can be toggled on the editor Viewport. Default off, not persisted, not in Player / Preview / Thumbnail. | **Pass** 2026-09-21 NVIDIA. Default `rate_mask=false` → `set-vrs-rate-mask enabled:true` → red 2×2 / dark 1×1 after a real orbit. Recapture: `dark_1x1=47328` `red_2x2=23248`. View menu `VRS Rate Mask`. Not in Player. Intel-only walk earlier: Fail (no attachment). |
| 3 | With VRS off or with no device extension: shading is today’s 1×1 clustered deferred. Editor and Play still open and orbit. No software VRS path. No FATAL. | **Pass** 2026-09-21. No-extension Intel + NVIDIA `BLUNDER_EDITOR_VRS=0`: 1×1 deferred, orbit, `software_vrs=false`, no FATAL. |
| 4 | G-buffer, shadows, transparent, outline, pick, SSAO, volumetric fog, and the open-progress overlay still look like today. Receiver ids are not smeared by 2×2, so Light linking does not jump to a neighbour MeshRenderer. | **Pass** 2026-09-21. Lighting/gizmos still clustered deferred se-world. NVIDIA live capture shows gizmos + checkerboard forest. 2×2 receiver smear not separately re-walked. |
| 5 | Play (`engine_player`) on the same scene is deferred clustered lighting. With the extension, that lighting triangle uses the same image-based VRS. The Player has no mask overlay. | **Pass** 2026-09-21 NVIDIA. Player se-world, FSR enabled, `VRS 1`, PrintWindow is the scene, no mask chrome. Intel-only walk earlier: Fail (no usable FB / no FSR). |
| 6 | Camera Preview, Mesh Preview, and Scene Thumbnail / Capture are deferred (same lighting-pass family VRS can attach to), not Forward opaque. Placement Preview stays Forward and is not rate-shaded. Headless / CLI / MCP do not need the extension to run. | **Pass** 2026-09-21 NVIDIA. 3× `DeferredRenderPath ready ... VRS 1`. Camera Preview overlay shows Main Camera (gpu-driven recull `cd5cacc`; first walk empty). Thumbnail matches that Main Camera wall (36970 bytes), not an empty RT. Live capture clustered deferred forest. Mesh inspector clipped by 2062×1118 window (layout). Placement stays `submitStandaloneMeshToRender` Forward. Headless MCP `query` PASS. No FATAL. M1 smoke: `attachment=true` (fail-disable path not hit). Intel-only walk earlier: Fail (empty preview / grey thumb / no FSR). |
| 7 | The walk is DogWalk `se-world.scene.asset`. Test and Sponza are not the demo Project. 138 Spot, a profiler “how much faster” number, and the dog walking are not required. Do not touch PR 24 or PR 36. | **Pass** 2026-09-21. se-world only. Did not touch PR 24 / 36 / 40 / 39. #36 already on `main` (`2f48679`); this branch rebased onto it. |
